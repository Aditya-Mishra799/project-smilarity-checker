# Application Configuration
# Base URL for the application
NEXT_PUBLIC_URL=http://localhost:3000
NEXTAUTH_URL_INTERNAL=http://localhost:3000

# Server mode (development/production) [Change to production for deployment, affects the JWT token storage in Next.js ]
SERVER_MODE=development

# MongoDB Configuration
# Full connection string for MongoDB (Atlas or local)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# Name of the database to use
DATABASE_NAME=project_similarity

# NextAuth Configuration
# Secret key for NextAuth session encryption (generate a strong random string)
NEXTAUTH_SECRET=your-super-secret-key-at-least-32-chars

# URL where the application is hosted (same as NEXT_PUBLIC_URL)
NEXTAUTH_URL=http://localhost:3000

# Email Configuration (Gmail)
# Gmail account email address
EMAIL_ID=your.email@gmail.com

# App-specific password generated from Gmail security settings
# Generate at: https://myaccount.google.com/apppasswords
EMAIL_PASSWORD=your-16-digit-app-password

# JWT Configuration
# Secret key for JWT token generation (generate a strong random string)
JWT_SECRET=another-super-secret-key-at-least-32-chars

# External API Configuration
# Base URL for the similarity checking API
NEXT_PUBLIC_SIMILARITY_API_ENDPOINT_BASE_URL=http://your-similarity-api.com/api/v1

# Note: Never commit the actual .env file to version control
# Keep this .env.example updated with all required variables but without actual values
